pipeline {
    agent any

    stages {
        stage('Setup') {
            steps {
                echo '🔧 Setup stage: initializing environment...'
            }
        }
        stage('Build') {
            steps {
                echo '🏗️ Build stage: compiling or preparing artifacts...'
            }
        }
        stage('Deploy') {
            steps {
                echo '🚀 Deploy stage: simulating deployment...'
            }
        }
        stage('Configure') {
            steps {
                echo '⚙️ Configure stage: applying configuration...'
            }
        }
        stage('Monitor') {
            steps {
                echo '📈 Monitor stage: checking system health...'
            }
        }
        stage('Validate') {
            steps {
                echo '✅ Validate stage: confirming everything works!'
            }
        }
 }   
}
